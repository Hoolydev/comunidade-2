import { Bookmark, MessageCircle, ThumbsUp } from "lucide-react";
import { posts } from "../../dados-comunidade";

export default function FeedPage() {
  return <div className="area-content"><section className="area-hero"><div className="area-hero-copy"><p className="area-eyebrow">Feed da comunidade</p><h1>Implementações, decisões e aprendizados reais.</h1><p>O espaço para compartilhar avanços, pedir ajuda e acompanhar o que está movimentando a comunidade.</p></div><div className="area-hero-aside"><strong>12</strong><span>novas conversas</span></div></section><div className="feed-layout"><div className="feed-list">{posts.map((post) => <article className="feed-card" key={post.title}><header><div className="feed-author"><i>{post.author.split(" ").map((part) => part[0]).join("").slice(0, 2)}</i><div><strong>{post.author}</strong><span>{post.time}</span></div></div><span className="status-pill">{post.tag}</span></header><h2>{post.title}</h2><p>{post.text}</p><footer><span><ThumbsUp size={14} /> 18 apoios</span> · <span><MessageCircle size={14} /> 6 respostas</span> · <span><Bookmark size={14} /> salvar</span></footer></article>)}</div><aside className="feed-side"><h3>Direção da semana</h3><p>Escolha uma tarefa repetitiva que consome mais de duas horas por semana. Mapeie entradas, decisão e saída antes de escolher a ferramenta.</p><span className="status-pill">Semana 32</span></aside></div></div>;
}
